**Project Name:** MySchedule

**Module Name:** Study Room, Playlist & Music, Settings 

**Created By:** Dang Trung Kien 

**Creation Date:** 2026-07-12 

**Reviewed By:** Team 5

**Reviewed Date:** 2026-07-12

### MODULE 1: STUDY ROOM (PHÒNG HỌC NHÓM)

| Test Case ID | Description | Precondition | Test Steps | Test Data | Expected Result | Actual Result | Status | Comments(if any) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TC-SR-01` | \[Unit\] Kiểm tra BVA tham gia phòng khi số lượng = max - 1 | Môi trường test (pytest) chạy | 1\. Mock DB để tạo 1 phòng có `max_participants = 5`.<br>2. Mock danh sách `current_users` đang có 4 user.<br>3. Gọi hàm `check_join_logic(room_id, user_5)`.<br>4. Assert (Kiểm tra) kết quả trả về của hàm. | `current_users = 4`, `max = 5` | Hàm trả về `True` (Thành công), không văng lỗi. |  | Pending | Áp dụng Boundary Value |
| `TC-SR-02` | \[Unit\] Kiểm tra BVA tham gia phòng khi đã đạt max | Môi trường test (pytest) chạy | 1\. Mock DB có 1 phòng với `max_participants = 5`.<br>2. Mock danh sách `current_users` có đúng 5 user.<br>3. Gọi hàm `check_join_logic(room_id, user_6)`.<br>4. Bắt exception/lỗi trả về. | `current_users = 5`, `max = 5` | Hàm ném lỗi "Phòng đã đầy" (Room is full). |  | Pending | Áp dụng Boundary Value |
| `TC-SR-03` | \[Unit\] Phân quyền gọi hàm `next_song` (Host) | Session gán quyền Host | 1\. Khởi tạo đối tượng `Room` với `host_id = 1`.<br>2. Thiết lập người gọi hàm (caller) có `user_id = 1`.<br>3. Gọi hàm `next_song()`.<br>4. Kiểm tra `current_song_index`. | Role = Host (ID=1) | Trạng thái bài hát chuyển sang index kế tiếp. |  | Pending | Decision Table |
| `TC-SR-04` | \[Unit\] Phân quyền gọi hàm `next_song` (Member) | Session gán quyền Member | 1\. Khởi tạo `Room` với `host_id = 1`.<br>2. Thiết lập caller có `user_id = 2` (Member).<br>3. Gọi hàm `next_song()`.<br>4. Bắt exception. | Role = Member (ID=2) | Hàm ném ra ngoại lệ `PermissionDenied`. |  | Pending | Decision Table |
| `TC-SR-05` | \[Unit\] Phân quyền gọi hàm `kick` (Host) | Session gán quyền Host | 1\. Khởi tạo `Room` (host_id=1), add Member (id=2).<br>2. Caller (id=1) gọi hàm `kick_member(target_id=2)`.<br>3. Kiểm tra mảng `current_users`. | Target ID = 2 | ID=2 bị xóa khỏi mảng `current_users`. |  | Pending | Decision Table |
| `TC-SR-06` | \[Unit\] Phân quyền gọi hàm `kick` (Member) | Session gán quyền Member | 1\. Khởi tạo `Room` (host_id=1), add Member 2, 3.<br>2. Caller (id=2) gọi hàm `kick_member(target_id=3)`.<br>3. Bắt exception quyền hạn. | Target ID = 3 | Hàm ném ngoại lệ `PermissionDenied`. |  | Pending | Decision Table |
| `TC-SR-07` | \[Unit\] Tăng `current_song_index` | Playlist có 3 bài (index 0,1,2) | 1\. Set `current_song_index = 0`.<br>2. Gọi hàm `next_song()`.<br>3. Trích xuất biến `current_song_index` mới. | `index = 0` | `current_song_index` cập nhật thành 1. |  | Pending | State Transition |
| `TC-SR-08` | \[Unit\] Lặp `current_song_index` ở cuối danh sách | Playlist có 3 bài (index 0,1,2) | 1\. Set `current_song_index = 2`.<br>2. Gọi hàm `next_song()`.<br>3. Trích xuất biến `current_song_index` mới. | `index = 2` | `current_song_index` quay vòng về 0. |  | Pending | Boundary Value |
| `TC-SR-09` | \[Integration\] Tạo phòng khi đang có phòng cũ mở | Client có JWT Token hợp lệ | 1\. Gửi `POST /api/rooms` tạo phòng A.<br>2. Lấy ID phòng A.<br>3. Gửi tiếp `POST /api/rooms` tạo phòng B.<br>4. Gửi `GET /api/rooms` kiểm tra trạng thái phòng A. | Tên phòng B: "Toán CC" | Trả về 201. Phòng A bị đổi status thành "closed", phòng B mở. |  | Pending | Flask test client |
| `TC-SR-10` | \[Integration\] Tạo phòng thiếu trường bắt buộc | Client có JWT Token hợp lệ | 1\. Chuẩn bị JSON payload rỗng `{}`.<br>2. Gửi request `POST /api/rooms`.<br>3. Bắt và đọc mã lỗi HTTP. | JSON rỗng `{}` | Trả về lỗi 400 Bad Request kèm message "Thiếu tên phòng". |  | Pending | Equivalence Partitioning |
| `TC-SR-11` | \[Integration\] Tham gia phòng không yêu cầu duyệt | Phòng có `require_approval=false` | 1\. User B gửi request `POST /api/rooms/:id/join`.<br>2. Kiểm tra HTTP Status Code trả về.<br>3. Kiểm tra DB xem User B có trong bảng members. | ID Phòng, Token User B | Nhận status 200 OK, User B có mặt trong phòng ngay lập tức. |  | Pending | Decision Table |
| `TC-SR-12` | \[Integration\] Tham gia phòng có yêu cầu duyệt | Phòng có `require_approval=true` | 1\. User B gửi request `POST /api/rooms/:id/join`.<br>2. Kiểm tra HTTP Status Code.<br>3. Check trạng thái User B trong DB là "pending". | ID Phòng, Token User B | Nhận status 202 (Accepted). Trạng thái chờ (waitlist). |  | Pending | Decision Table |
| `TC-SR-13` | \[Integration\] Cơ chế Polling `/sync` | User đã ở trong phòng | 1\. Gửi request `GET /api/rooms/:id/sync`.<br>2. Phân tích JSON response trả về.<br>3. Kiểm tra các field: `current_song`, `members`. | ID phòng hợp lệ | JSON trả về khớp với dữ liệu thực tế đang lưu trong database. |  | Pending | State Transition |
| `TC-SR-14` | \[Integration\] Member chủ động rời phòng | User là Member trong phòng | 1\. Gửi request `POST /api/rooms/:id/leave`.<br>2. Kiểm tra response code.<br>3. Gửi `/sync` để xác minh User không còn trong list. | Mức độ user | 200 OK. Danh sách `members` bị trừ đi 1 người. |  | Pending | State Transition |
| `TC-SR-15` | \[Integration\] Host chủ động rời phòng | User là Host tạo phòng | 1\. Host gửi `POST /api/rooms/:id/leave`.<br>2. Đăng nhập User B (Member), gọi `GET /sync`.<br>3. Kiểm tra mã lỗi trả về cho User B. | Mức độ Host | Phòng bị đóng. User B gọi `/sync` nhận lỗi 404 (Phòng không tồn tại). |  | Pending | State Transition |
| `TC-SR-16` | \[Integration\] Kick user không tồn tại | User là Host | 1\. Gửi request `POST /api/rooms/:id/kick/99999`.<br>2. Bắt HTTP response code. | Target ID = 99999 | Trả về mã lỗi 404 Not Found (Không tìm thấy user trong phòng). |  | Pending | Error Guessing |
| `TC-SR-17` | \[Integration\] Chuyển bài khi playlist trống | Host ở phòng không có nhạc | 1\. Gửi request `POST /api/rooms/:id/next_song`.<br>2. Bắt HTTP response code và xem server có crash không. | Playlist rỗng | Trả về 400 Bad Request, server hoạt động bình thường. |  | Pending | Error Guessing |
| `TC-SR-18` | \[System\] Luồng E2E duyệt user vào phòng | 2 User mở 2 trình duyệt riêng | 1\. Trình duyệt A: Host tạo phòng (yêu cầu duyệt).<br>2. Trình duyệt B: User bấm "Join".<br>3. Trình duyệt A: Nhấn "Accept" trên thông báo góc màn hình.<br>4. Trình duyệt B: Đợi UI chuyển vào không gian học. | Thao tác UI E2E | Trình duyệt B tải thành công giao diện phòng học sau khi Host ấn Accept. |  | Pending | Cypress E2E |
| `TC-SR-19` | \[System\] Đồng bộ UI nhạc qua Polling | 2 trình duyệt song song (1 phòng) | 1\. Trình duyệt A (Host): Bấm nút "Next Song".<br>2. Bấm giờ đếm ngược 3 giây.<br>3. Trình duyệt B (Member): Quan sát tên bài hát đang phát. | Thao tác UI E2E | Trình duyệt B tự động nhảy sang tên/âm thanh bài mới (do `/sync`). |  | Pending | System/Polling |
| `TC-SR-20` | \[System\] Dùng token cũ sau khi bị kick | User B vừa bị Host kick | 1\. Copy JWT Token của User B trước khi bị kick.<br>2. Host kick User B.<br>3. Dùng Postman nhét Token B gọi `GET /sync`. | Expired Authorization | Server chặn request, trả về 403 Forbidden. |  | Pending | Security/Bảo mật |

---

### MODULE 2: PLAYLIST, MUSIC & AI CHATBOT

| Test Case ID | Description | Precondition | Test Steps | Test Data | Expected Result | Actual Result | Status | Comments(if any) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TC-PLY-01` | \[Unit\] Kiểm tra hàm sinh prompt AI | Thư viện `google.genai` được mock | 1\. Gọi hàm `build_greeting_prompt(time, weather)`.<br>2. Truyền tham số: time="Morning", weather="Rain".<br>3. Phân tích chuỗi trả về từ hàm. | `time=Morning`, `weather=Rain` | Chuỗi kết quả chứa từ khóa "buổi sáng", "trời mưa". |  | Pending | Kiểm tra nội dung prompt |
| `TC-PLY-02` | \[Integration\] Upload file mp3 hợp lệ | Token hợp lệ | 1\. Tạo HTTP POST form-data tới `/api/music/upload`.<br>2. Đính kèm file `test.mp3`.<br>3. Gửi request và check response URL trả về. | File `.mp3` 3MB | Status 201 Created, trả về URL tĩnh trỏ tới file nhạc. |  | Pending |  |
| `TC-PLY-03` | \[Integration\] Upload sai định dạng | Token hợp lệ | 1\. Tạo HTTP POST form-data tới `/api/music/upload`.<br>2. Đính kèm file `virus.exe`.<br>3. Gửi request và check response code. | File `.exe` 1MB | Status 400 Bad Request, báo lỗi "Định dạng không hỗ trợ". |  | Pending | Equivalence Partitioning |
| `TC-PLY-04` | \[Integration\] Add nhạc bằng link YouTube | Token hợp lệ | 1\. POST `/api/music` với JSON body chứa `url`.<br>2. Query DB kiểm tra table `music_links`. | `url: "youtube.com/watch?v=..."` | Status 201 Created, record mới được lưu vào database. |  | Pending |  |
| `TC-PLY-05` | \[Integration\] Add nhạc bằng link rác | Token hợp lệ | 1\. POST `/api/music` với JSON body chứa `url` rác.<br>2. Kiểm tra response nhận về. | `url: "not_a_link_123"` | Status 400 Bad Request, báo lỗi URL không đúng chuẩn. |  | Pending | Error Guessing |
| `TC-PLY-06` | \[Integration\] Tạo playlist ký tự đặc biệt | Token hợp lệ | 1\. Gửi POST `/api/playlists` với tham số `name`.<br>2. Gọi API GET kiểm tra lại tên playlist vừa lưu. | `name: "Lofi @#2026"` | 201 Created, Playlist tạo thành công và giữ nguyên ký tự `@#`. |  | Pending | XSS/Validation test |
| `TC-PLY-07` | \[Integration\] Thêm bài vào playlist | Có sẵn 1 Playlist rỗng | 1\. Gửi POST `/api/playlists/:id/items` với `music_id=1`.<br>2. Gọi API xem chi tiết playlist để đối chiếu số lượng. | `music_id = 1` | 201 Created, mảng items của playlist có độ dài = 1. |  | Pending | State Transition |
| `TC-PLY-08` | \[Integration\] Thêm bài trùng lặp | Playlist đã có bài `music_id=1` | 1\. Gửi POST `/api/playlists/:id/items` với `music_id=1`.<br>2. Kiểm tra JSON trả về. | `music_id = 1` | Báo lỗi 400 (Bài hát đã tồn tại) hoặc tự động bỏ qua (Tuỳ thiết kế). |  | Pending | Error Guessing |
| `TC-PLY-09` | \[Integration\] Xoá playlist sở hữu | User là chủ playlist | 1\. Gửi DELETE `/api/playlists/:id`.<br>2. Gửi GET để thử lấy lại thông tin playlist đó. | Valid Playlist ID | DELETE trả 200 OK. GET trả 404 Not Found. |  | Pending |  |
| `TC-PLY-10` | \[Integration\] Xoá playlist người khác | Token User A, ID thuộc User B | 1\. Gửi DELETE `/api/playlists/:id_of_userB`.<br>2. Kiểm tra HTTP Status. | Other User's ID | Trả về 403 Forbidden. Không xoá dữ liệu của người khác. |  | Pending | Security/Quyền hạn |
| `TC-PLY-11` | \[Integration\] API `ai_status` OFF | `.env` set `ENABLE_AI = False` | 1\. Gửi GET `/api/ai_status`.<br>2. Đọc field `status` trong JSON. | N/A | Trả về `{ status: false }`. |  | Pending | State |
| `TC-PLY-12` | \[Integration\] API `ai_status` ON | `.env` set `ENABLE_AI = True` | 1\. Gửi GET `/api/ai_status`.<br>2. Đọc field `status` trong JSON. | N/A | Trả về `{ status: true }`. |  | Pending | State |
| `TC-PLY-13` | \[Integration\] Gửi Chatbot Text hợp lệ | Mock Gemini API trả về "Hello" | 1\. Gửi POST `/api/chatbot/chat` với body JSON chứa `message`.<br>2. Kiểm tra `reply` từ server. | `message: "Xin chào"` | Trả về `{reply: "Hello"}`, HTTP 200. |  | Pending | Mocking GenAI |
| `TC-PLY-14` | \[Integration\] Gửi Chatbot câu trống | API `ai_status=true` | 1\. Gửi POST `/api/chatbot/chat` với `message` trống rỗng.<br>2. Quan sát HTTP response. | `message: " "` | 400 Bad Request, API không gọi lên Gemini để tiết kiệm token. |  | Pending | Boundary Value |
| `TC-PLY-15` | \[Integration\] Xử lý lỗi Timeout từ Gemini | Mock Gemini ném exception Timeout | 1\. Gửi POST `/api/chatbot/chat`.<br>2. Kiểm tra log server và HTTP response. | `message: "Test"` | 503 Service Unavailable, trả message duyên dáng, server không bị crash. |  | Pending | Error Guessing |
| `TC-PLY-16` | \[System\] Thao tác UI Chatbot | UI Dashboard | 1\. Focus vào ô input của component Chatbot.<br>2. Nhập "Hôm nay học gì?".<br>3. Nhấn phím Enter.<br>4. Quan sát UI. | "Hôm nay học gì?" | DOM render text "AI đang gõ...", sau đó hiển thị kết quả. |  | Pending | Cypress E2E |
| `TC-PLY-17` | \[System\] Lời chào AI Buổi Sáng | Máy tính set giờ 8 AM | 1\. Chỉnh system clock về 08:00 AM.<br>2. Đăng nhập và mở Dashboard.<br>3. Quan sát Tooltip/Toast của Ketib AI. | System Time = 08:00 | AI render thông báo chứa nội dung chào buổi sáng. |  | Pending | Kịch bản ngữ cảnh |
| `TC-PLY-18` | \[System\] Lời chào AI Buổi Tối | Máy tính set giờ 10 PM | 1\. Chỉnh system clock về 22:00 PM.<br>2. Refresh trang (F5).<br>3. Quan sát Tooltip/Toast của Ketib AI. | System Time = 22:00 | AI render thông báo chúc buổi tối/chúc ngủ ngon. |  | Pending | Kịch bản ngữ cảnh |
| `TC-PLY-19` | \[System\] Xoá nhạc khi đang phát trong phòng | Trình duyệt mở 2 tab | 1\. Tab 1 (Study Room): Đang nghe bài X.<br>2. Tab 2 (Quản lý): Xoá bài X khỏi database.<br>3. Tab 1: Đợi chu kỳ `/sync`. | Thao tác chéo Tab | Tab 1 tự động chuyển sang bài tiếp theo hoặc văng cảnh báo "Bài hát đã bị xoá". |  | Pending | End-to-end integration |

---

### MODULE 3: SETTINGS (CÀI ĐẶT & GIAO DIỆN)

| Test Case ID | Description | Precondition | Test Steps | Test Data | Expected Result | Actual Result | Status | Comments(if any) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TC-SET-01` | \[Integration\] Lấy Setting Default | Account mới tạo tinh | 1\. Gửi GET `/api/settings`.<br>2. Kiểm tra JSON response trả về so với cấu hình mặc định. | Không có data cũ | Trả về `{theme: 'light', background_url: 'default'}`. |  | Pending |  |
| `TC-SET-02` | \[Integration\] Cập nhật Dark Theme | Token hợp lệ | 1\. Gửi PUT `/api/settings` với JSON payload.<br>2. Gọi GET `/api/settings` lần 2.<br>3. So sánh kết quả. | `{theme: 'dark'}` | 200 OK. Lần gọi GET thứ 2 trả về theme là "dark". |  | Pending |  |
| `TC-SET-03` | \[Integration\] Cập nhật background URL | Token hợp lệ | 1\. Gửi PUT `/api/settings` với JSON payload.<br>2. Truy vấn Database bảng users để kiểm tra giá trị. | `{bg: 'http://.../img.jpg'}` | 200 OK. Database cập nhật column background đúng URL. |  | Pending |  |
| `TC-SET-04` | \[Integration\] PUT Setting không có token | Không đính kèm header Bearer | 1\. Gửi PUT `/api/settings` với payload theme.<br>2. Kiểm tra mã trạng thái. | `{theme: 'dark'}` | Trả về 401 Unauthorized, thao tác bị từ chối. |  | Pending | Security / Auth |
| `TC-SET-05` | \[Integration\] PUT chuỗi quá dài (Over limits) | Token hợp lệ | 1\. Tạo một URL giả định dài 10,000 ký tự.<br>2. Gửi PUT `/api/settings`.<br>3. Đánh giá phản hồi server. | URL &gt; 10000 chars | API chặn và trả 400 Bad Request, ngăn lỗi tràn Database. |  | Pending | BVA, chống tràn DB |
| `TC-SET-06` | \[Integration\] Phản hồi Open-Meteo: Mưa | Mock API thời tiết trả mã code 61 | 1\. Gửi request `/api/auth/ai-helper` (lấy ngữ cảnh).<br>2. Phân tích tham số background gửi về client. | `code=61 (Rain)` | API set gợi ý background/theme tĩnh mang yếu tố "Mưa". |  | Pending | Mock Open-Meteo |
| `TC-SET-07` | \[Integration\] Phản hồi Open-Meteo: Nắng | Mock API thời tiết trả mã code 0 | 1\. Gửi request `/api/auth/ai-helper` (lấy ngữ cảnh).<br>2. Phân tích tham số background gửi về client. | `code=0 (Clear)` | API set gợi ý background/theme tĩnh mang yếu tố "Nắng". |  | Pending | Mock Open-Meteo |
| `TC-SET-08` | \[Integration\] Lỗi mạng khi gọi Open-Meteo | Mock thư viện requests ném timeout | 1\. Gửi request lấy thông tin trang chủ.<br>2. Xem server có crash không. | Exception (Timeout) | Hệ thống fallback về thời tiết/background mặc định. HTTP 200 OK. |  | Pending | Error Guessing |
| `TC-SET-09` | \[System\] Chuyển đổi giao diện React tức thì | Giao diện đang là Light Theme | 1\. Truy cập màn hình Settings.<br>2. Bấm vào công tắc "Dark Theme".<br>3. Quan sát sự thay đổi trên màn hình. | UI Toggle Switch | Giao diện React đổi sang màu tối lập tức (nhờ State), không bị tải lại trang. |  | Pending | UI Component state |
| `TC-SET-10` | \[System\] Lưu trạng thái UI sau tải lại trang | Đã đổi ảnh nền A trong cài đặt | 1\. Đổi background URL trên UI.<br>2. Nhấn nút F5 để làm mới (refresh) trình duyệt.<br>3. Kiểm tra ảnh nền khi trang load xong. | Bấm F5 (Refresh) | Ảnh nền A vẫn được áp dụng (React Fetch lại API thành công). |  | Pending | Cypress E2E |
| `TC-SET-11` | \[System\] Áp dụng Theme vào Phòng Học | Đang dùng Dark Theme ở Dashboard | 1\. Đổi sang Dark Theme trên cài đặt.<br>2. Bấm vào điều hướng để mở tab Study Room.<br>3. Quan sát giao diện phòng học. | Navigation | Giao diện phòng học kế thừa CSS Dark Theme đồng bộ. |  | Pending | UI Compatibility |
| `TC-SET-12` | \[System\] Đồng bộ Settings qua nhiều tab | Mở 2 Tab trình duyệt (Cùng 1 session) | 1\. Tab 1: Đổi ảnh nền thành B.<br>2. Tab 2: Refresh trang (F5).<br>3. Quan sát Tab 2. | Thao tác song song | Tab 2 nhận được ảnh nền B ngay sau khi tải lại. |  | Pending |  |
| `TC-SET-13` | \[System\] **E2E Role D Scenario (Kịch bản cuối)** | Có 2 trình duyệt đóng vai Host, Member | 1\. Tab 1 (Host): Tạo phòng, chọn Playlist, đổi Dark Theme.<br>2. Tab 2 (Member): Nhấn Join.<br>3. Tab 1: Duyệt Join.<br>4. Tab 1: Bấm "Next Song".<br>5. Tab 2: Gõ chat hỏi AI. | End-to-end full flow | Chuỗi 5 thao tác phản hồi chính xác, đồng bộ bài hát trơn tru, chat AI đáp ứng ngay cả khi phòng đang phát nhạc. |  | Pending | Kiểm thử thủ công hệ thống |
| `TC-SET-14` | \[System\] Lỗi khi mất mạng cục bộ (Offline) | Chạy UI, DevTools set network "Offline" | 1\. Vào Settings, sửa thông tin.<br>2. Bấm nút Lưu (Save).<br>3. Quan sát phản hồi UI. | Network = Offline | UI ném một Toast/Alert: "Lỗi kết nối mạng", không cập nhật sai state hiển thị. |  | Pending | Robustness / Error Guessing |
| `TC-SET-15` | \[Unit\] Hàm Parser Toạ độ VN (Helper Utils) | Module parse JSON của thời tiết | 1\. Cung cấp file JSON payload fake.<br>2. Gọi hàm `extract_vietnam_coordinates(json)`.<br>3. So sánh dữ liệu bóc tách được. | Tọa độ (Long, Lat) | Trả về một Tuple (Long, Lat) đúng format được yêu cầu. |  | Pending | Logic xử lý dữ liệu thô |
| `TC-SET-16` | \[Integration\] Test CORS Domain giới hạn | Dùng Postman | 1\. Gửi request GET `/api/settings`.<br>2. Chèn Header `Origin: http://hacker.com`.<br>3. Đọc phản hồi. | `Origin: hacker.com` | Trình duyệt giả lập (Postman) nhận lỗi CORS / Blocked. |  | Pending | CORS Security |

---

### MODULE 4: PERFORMANCE TESTING (KIỂM THỬ HIỆU NĂNG)

*Sử dụng công cụ Locust để giả lập tải (Load), ép tải (Stress) và kiểm tra rò rỉ bộ nhớ (Soak/Long sequence).*

| Test Case ID | Description | Precondition | Test Steps | Test Data | Expected Result | Actual Result | Status | Comments(if any) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TC-PERF-01` | **\[Baseline Test\]** Đo lường thời gian phản hồi (Response Time) cơ bản của các API CRUD. | Môi trường Staging đã khởi động. DB có sẵn 50 users. | 1\. Chạy Locust script giả lập 10 Users đồng thời (Concurrent Users).<br>2. Các user thực hiện kịch bản: Login -&gt; Lấy list Tasks -&gt; Tạo 1 Task -&gt; Đăng xuất.<br>3. Chạy trong 5 phút. | 10 Concurrent Users | Thời gian phản hồi trung bình (Avg Response Time) &lt; 500ms. Tỷ lệ lỗi (Error rate) = 0%. |  | Pending | Xác định Baseline để so sánh. |
| `TC-PERF-02` | **\[Load Test\]** Kiểm tra chịu tải cơ chế Polling `/sync` trong Study Room. | Môi trường Staging. Khởi tạo sẵn 10 Study Rooms. | 1\. Thiết lập 100 Virtual Users trên Locust.<br>2. Chia 100 user vào 10 phòng học (mỗi phòng 10 người).<br>3. Script giả lập gọi `GET /api/rooms/:id/sync` liên tục mỗi 3 giây.<br>4. Duy trì trong 15 phút. | 100 Users, chu kỳ 3s/request | Hệ thống không bị crash. Thời gian phản hồi của `/sync` &lt; 1 giây. CPU của backend Server &lt; 75%. |  | Pending | Đây là API dễ gây nghẽn cổ chai nhất hệ thống. |
| `TC-PERF-03` | **\[Stress Test\]** Ép tải chức năng Đăng nhập/Đăng ký (Tính toán mã hóa Bcrypt). | Môi trường Staging. DB có sẵn 10,000 user. | 1\. Dùng Locust ramp-up (tăng tải dần) lượng User từ 10 lên 200 Users.<br>2. Tất cả user liên tục gửi request `POST /api/auth/login`.<br>3. Theo dõi System Monitor (CPU/RAM). | 200 Concurrent Users login liên tục | Có thể xảy ra hiện tượng CPU tăng vọt (Processor bottleneck), nhưng hệ thống không sập. Các request vượt quá khả năng xử lý sẽ trả về 503 duyên dáng. |  | Pending | Đo ngưỡng tới hạn (Breaking point) của CPU. |
| `TC-PERF-04` | **\[Stress Test\]** Kiểm tra giới hạn tần suất (Rate Limiting) của Flask-Limiter. | Flask-Limiter đang set cấu hình giới hạn 100 request/phút/IP. | 1\. Dùng 1 User ảo (1 IP duy nhất).<br>2. Bắn 120 requests liên tục vào API `GET /api/tasks` trong vòng 1 phút.<br>3. Quan sát HTTP Status Code trả về. | 120 reqs/phút từ 1 IP | 100 request đầu tiên trả về 200 OK. Từ request 101 trở đi trả về 429 (Too Many Requests). Hệ thống không sập. |  | Pending | Kiểm tra cơ chế tự vệ của Backend. |
| `TC-PERF-05` | **\[Performance\]** Độ trễ API bên thứ 3 (Gemini AI Chatbot). | Backend có kết nối API thật của Gemini (Quota riêng cho test). | 1\. Giả lập 20 Users đồng thời gửi câu hỏi vào `POST /api/chatbot/chat`.<br>2. Đo lường thời gian (Process time + Transportation time). | Payload text đa dạng | Hệ thống xử lý bất đồng bộ tốt, không bị block các API khác. Thời gian phản hồi chatbot có thể trễ (ví dụ &lt; 5s) nhưng không làm treo Backend. |  | Pending | Kiểm thử Processing Delays từ ngoại vi. |
| `TC-PERF-06` | **\[Long Sequence/Soak Test\]** Kiểm tra rò rỉ bộ nhớ (Memory Leak). | Môi trường Staging. System Monitor đang ghi nhận RAM. | 1\. Thiết lập 50 Users hoạt động tuần hoàn (Login -&gt; Get Data -&gt; Logout).<br>2. Chạy kịch bản liên tục trong **12 giờ** (hoặc 24 giờ).<br>3. Theo dõi biểu đồ Available Bytes và Process Working Sets. | 50 Users, chạy trong 12h | Dung lượng RAM khả dụng (Available Memory) duy trì ổn định (&gt; 128MB). Không có hiện tượng RAM bị nuốt dần theo thời gian (Memory Leak). |  | Pending | Kiểm tra tính ổn định lâu dài. |
| `TC-PERF-07` | **\[Spike Test\]** Tải đột biến khi hệ thống đổi Theme/Thời tiết đồng loạt. | Có mock Open-Meteo API. | 1\. Đặt lịch hệ thống chuyển sang giờ Tối (hoặc thời tiết đổi sang Mưa).<br>2. 150 client đồng loạt gọi request lấy cấu hình thời tiết/theme.<br>3. Đánh giá tốc độ phản hồi. | 150 Users đồng loạt req | Hệ thống vẫn phản hồi `< 2s` cho toàn bộ các truy vấn cấu hình đồng loạt, không bị nghẽn (Deadlock) ở Database. |  | Pending |  |
